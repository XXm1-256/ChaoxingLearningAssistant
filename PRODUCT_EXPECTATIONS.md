# 最终预期：课程全程连续播放

## 一句话目标

程序持续开启后，从当前选择的位置开始，按课程顺序处理全部真实视频：同章节内依次播放所有视频，跳过测验/作业/签到/考试，再进入下一章节，直到最后一个视频自然结束。

## “全部视频播放完成”的判定

每条识别到的真实视频至少满足一项：

1. 平台在开始前已经明确标记为完成；或
2. 本次运行中观察到真实播放器持续播放并自然结束。

状态未知的视频不能被当成已完成，必须进入播放候选。程序不伪造时长、进度或平台完成标记。

## 固定播放顺序

1. 当前章节的当前视频自然结束。
2. 查找当前章节后续真实视频，并按视频出现顺序播放。
3. 章节任务点中出现测验、测试题、作业、资料、签到或考试时跳过。
4. 当前章节所有视频均完成后，进入后续章节的第一个待播视频。
5. 没有视频的章节直接越过。
6. 最后一个可达视频自然结束且全课程无待播视频时，才显示课程播放完成。

## 界面预期

- 当前章节：显示真实章节标题。
- 当前视频：显示 `第 2/4 个视频 · 视频标题`。
- 标题不可识别时：显示 `第 2/4 个视频`，不显示笼统的“播放视频”。
- 视频编号只统计视频；测试、作业和资料不占视频编号。
- 内部仍保留原始任务点顺序，保证跳转准确并便于日志定位。

## 自动恢复预期

- 新播放器出现较慢时有界重试播放并恢复倍速。
- 章节点击只改变高亮、播放器未变化时，继续尝试网页原生动作或上下文导航。
- 登录、验证码、人脸或学校认证出现时暂停，人工完成后从最近确认的视频继续。
- 浏览器明确阻止自动播放时保留正确章节并提示一次，不循环乱点。

## 当前实现状态（v1.36）

- 懒加载视频 iframe 即使只显示通用 `index.html`，也会从 class、data 和 _src 等真实元数据识别视频及名称；同页测验不会进入视频队列。
- “打开未完成章节”具有执行中与结果提示；缺少明确状态时只选择已有视频证据的待核验章节。
- 课程卡不再把返回链接附近的“提示”标题当成课程；进入学习页会显示真实当前课程。
- 播放过程中显示下一视频。同章节多视频优先继续本章节，跨章节时跳过明确测验、作业、考试、签到、资料和已完成视频。
- 学生主界面按确认参考图采用浅色课程目录、深色中央播放区和深色运行状态区；正式名称为“学习通课程视频播放助手”。
- 章节总状态未完成但视频任务均已完成时不进入待播队列；测验未完成不会诱发视频章节误跳。
- 普通模式以 80% 显示学习通网页；F11 隐藏外围界面并让网页铺满窗口，Esc 或 F11 返回。

- 课程列表明确排除“返回课程”“课程首页”“我的课程”等导航文字，只保留真实课程候选。
- 学生界面使用真实软件名与功能文案，日志保留并以“日志与反馈”“反馈包”呈现。
- 公开教学同时提供 GitHub 可阅读的 Markdown、可下载的 DOCX 和无需 Word 的 PDF。

已具备：自然结束识别、同章节视频优先、测试类任务跳过、下一章节切换、已完成章节跳过、状态未知视频保留、浏览器自动播放许可、播放状态重试、手动切章后自动播放、真实播放器证据校验、章节内 `第 N/M 个视频` 显示、课程级待播清单、无视频章节确认跳过、结束前全局复核。

界面辅助：默认浅绿护眼主题；按钮按压反馈；关键功能结果提示；首次教学和可重复打开的教学入口。

仍需实测：学校定制页面中的视频编号完整性；多个无视频章节连续跨越；真实账号环境长时间连续运行至课程结束。
