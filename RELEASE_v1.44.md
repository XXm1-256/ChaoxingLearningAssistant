# v1.44 手动选择起点与自动续播 2026 09 12

## 宝贝要求

- 删除学生界面上的“打开未完成章节”。
- 由同学在学习通网页中手动选择没看的视频。
- 程序保留当前视频结束后自动播放下一视频的能力。

## 修改内容

- 删除“打开未完成章节”按钮和仅供该按钮使用的事件入口。
- “开始”只读取并播放网页中已经手动打开的当前视频，不再主动搜索、跳转或猜测未完成章节。
- 自然结束后的同章下一视频、跨章节续播、测验排除、目标播放器锁定和失败停住逻辑保持不变。
- 使用教学与常见问题改为“手动选择起点，再点开始”，不再出现已删除按钮。

## Debug 原理

起始位置由同学在学习通真实页面中确定。程序只在播放器已经出现后接管该播放器，并保存其真实身份。视频自然结束后，续播链再按真实任务顺序寻找下一段视频。

## 为什么这样修改

学习通的章节完成状态可能分批加载，也可能包含测验或作业。删除主动寻找入口后，程序不再替同学猜测起点，减少误跳和误报；自动续播仍解决观看过程中重复点击的问题。

## 验证范围

- 静态检查确认按钮、事件入口和“开始”中的主动寻找代码均已删除。
- Adapter JavaScript 与 MSTest 继续验证同章多视频、跨 iframe、测验排除和目标播放器锁定。
- 真实课程仍需验证同章第二视频与章末跨章节续播。
