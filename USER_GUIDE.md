# v1.44 使用说明

## v1.47 进度和倍速说明

课程进度统计全部任务点，仅显示平台明确提供的数量，读不到时显示尚未读取。右侧是当前视频播放位置，未读到时长时显示正在读取。不能继承倍速时使用正常速度；90秒等待后最多再尝试一次正常速度播放，仍失败请人工处理。完整说明见 RELEASE_v1.47.md。

1. 将源码 ZIP 完整解压到新目录，不要覆盖旧版本，也不要直接在 ZIP 内运行。
2. 普通下载版先打开最外层 `使用教学.pdf` 或 `使用教学.docx`；异常处理见同目录 `常见问题与处理方法`。再进入 `学习通课程视频播放助手` 文件夹双击程序；源码版可运行 `BUILD_V1_44.bat` 或 `START.bat`。
3. 登录学习通并进入课程后，在网页中手动点开准备观看的视频。视频页面出现后，再点“开始”。
4. 左侧章节点击只有在真实播放器发生对应变化后才确认；确认后会等待播放器就绪并自动播放。
5. “开始”从当前手动选择的视频接管播放。目录状态尚未稳定时会继续读取，不会用不完整状态提前判断课程结束。
6. 视频自然结束后，程序先找同章节后续真实视频并跳过测试/作业等任务。新任务点只有在播放器真实加载后才确认成功，自动播放请求会定向发送给新播放器；跨章节时继续复核真实视频状态。
7. 学习通切换播放器通常需要约半分钟。显示“下一视频加载中”时保持页面开启，各加载或播放确认阶段最多等待 90 秒，成功后立即继续。不要反复点击；视频时间实际增长后才算播放成功。
7. 程序会从本次起点建立课程待播清单；无视频章节可跳过，打不开的章节会被记录并在最后阻止提前显示完成。
8. 最近一次有效课程列表会缓存 30 天；网页暂时扫描不到课程时不会立即清空左侧。
9. 今日统计只累计程序实际观察到播放器处于播放状态的时间，暂停和长时间休眠不计入。
10. 若仍有错位或不跳，导出反馈包并保留 `CX-PLAYER-SWITCH`、`CX-AUTO-NEXT*`、`CX-COURSE-*`、`CX-VIDEO-TASK-SCAN`、`CX-CHAPTER*` 日志。
11. 首次启动会显示四步教学；关闭后可点主界面右上角 `?` 再次打开。按钮下沉表示点击已经发生，右上提示卡表示功能状态已经改变。

程序不伪造学习时长/完成状态，不自动答题、考试、签到，也不绕过验证码、人脸、学校 SSO 或风控。
