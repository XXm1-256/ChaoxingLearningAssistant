# v1.38 课程进入与章节目录自动加载 2026 09 12

## 宝贝要求了什么

单击左侧课程后，应当直接加载并显示章节目录，不应要求先在学习通网页中手动打开一个章节。章节目录尚未加载时，“打开未完成章节”也不能直接按空列表误报没有视频。

## 为什么这样要求

课程目录是后续识别未完成视频、同章多视频和自动顺序播放的入口。若程序只在具体章节页面刷新目录，课程刚打开时所有自动功能都会拿到空列表，界面看起来像失效。

## 姐姐怎样修复

- 课程卡由双击改为单击进入，避免普通点击只改变顶部课程名称。
- 单击课程后立即切换到章节页签，清空上一门课程的旧章节，并显示正在读取状态。
- 将 `studentcourse`、`visit/courses` 等普通课程工作页纳入章节目录扫描范围。
- 初次目录为空时短暂重试；异步目录仍未出现时，后台轮询继续补读。
- 原有“打开未完成章节”逻辑不放宽视频判断，继续排除测验和作业；修复发生在它上游的目录加载阶段。

## Debug 原理

原流程把“课程页”和“具体章节页”混成一个条件：只有地址已含章节 ID 才持续刷新目录。现在单独判断页面是否可能包含课程目录，因此课程首页也能等待异步 iframe。测试同时包含两个课程工作页正例和普通学习通首页反例，避免在无关首页无限扫描。

## 验证结果

- MSTest：37/37 通过。
- Adapter JavaScript：52/52 通过。
- 完整构建、两份 ZIP、MASTER 与 GitHub 同步结果见 `TEST_REPORT.md`。
- 真实学习通账号和学校定制课程页仍需成品复测。
