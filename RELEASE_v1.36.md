# v1.36 懒加载视频任务识别与章节打开反馈 2026 09 11

## 用户要求与原因

- 播放时“下一视频”仍显示“未发现待播放的下一视频”。
- 点击“打开未完成章节”没有可见反应。
- 需要继续排查相关问题，同时坚持只识别真实视频任务，不能把章节测验当成下一视频。

这些现象说明上版虽然能识别已经生成的 `<video>`，但没有完整覆盖学习通常见的懒加载任务：父页面只放置 iframe，iframe 的 `src` 可能只是 `index.html`，真正类型、标题和媒体 ID 藏在 `class`、`data`、`_src`、`objectid` 或 `mid` 中。

## 实现

- 视频任务扫描同时读取 iframe 的 `class`、`data`、`_src`、`module`、`objectid` 与 `mid`。
- 支持从 iframe 的 JSON `data` 中读取视频名称，使“下一视频”能显示真实标题。
- 明确识别 `ans-insertvideo-online`、`richvideo`、`module: video` 等懒加载视频证据。
- 对 `workid`、`worktype`、`module: work`、章节测验、作业、考试、签到继续执行排除，防止误把题目当视频。
- 同章切换与“打开未完成章节”共用相同视频证据，必要时点击真实 iframe 或任务块。
- “打开未完成章节”点击后立即显示“正在查找”，执行期间禁用按钮以防重复点击；找到目标后显示正在进入的章节，找不到时给出可执行说明。
- 明确未完成标记不存在时，只回退到已有真实视频证据的待核验章节，不凭章节总状态猜测视频。

## Debug 原理

本轮没有通过延长等待或重复乱点掩盖问题，而是建立与真实页面一致的最小 DOM：视频 iframe 的可见 `src` 为 `index.html`，类型位于 `class/data/_src`；旁边同时存在未完成章节测验。测试要求扫描结果只留下视频，能读取标题和媒体 ID，并且“打开未完成”动作只点击视频 iframe。

## 验证边界

- 内嵌适配脚本回归覆盖懒加载视频与测验并存页面。
- WPF 编译与 MSTest 验证程序结构和事件绑定。
- 自动测试不能代替具体学校账号页面；若实测模板仍不同，应导出反馈包，依据真实日志增加最小适配。

