# v1.28 同章节多视频优先修复（2026-09-11）

## 宝贝为何提出、当时要求什么

一个章节可能含多个视频，中间还夹着章节测验。旧流程可能看完第一个视频就切章，或者顺着“下一任务”误入测试。宝贝要求本章视频优先按顺序播放，测验、作业、签到和考试不进入视频队列，本章视频处理完后才切章。

## 姐姐怎样处理与 Debug 原理

姐姐将父页面任务块与跨 iframe 视频合并成有顺序的证据链，并把“同章后续视频扫描”提到所有跨章动作之前。Debug 原理是先建立可验证的优先级，再用“视频—测试—视频”模拟 DOM 回归确认测试会被跳过、后续视频仍会命中；模糊的“下一节/下一任务”控件不再当成视频按钮。

## 目标规则

当前视频自然结束后，严格按以下顺序处理：

1. 在当前章节内部寻找排在后面的真实视频。
2. 跳过夹在视频之间的测试、测验、作业、签到和考试任务。
3. DOM 结构无法识别时，只允许点击明确写着“下一视频”的播放器控件。
4. 当前章节确认没有后续视频后，才进入下一章节并自动播放。

## ERR-AUTO-ORDER-001：章节内视频未播完就切章或进入测试

- 现象：章节包含多个视频和测试题时，一个视频结束后可能直接切到下一章，或者网页的通用“下一项”按钮把页面带到测试题。
- 根因一：旧控制流先点击播放器“下一视频/下一节”，之后才检查当前章节内的其它视频。
- 根因二：同章节候选只接受明确未完成的视频；状态未知但真实存在的后续视频会被忽略。
- 根因三：播放器按钮识别同时接受“下一节”“下一个”和通用 next 类名，无法保证目标是视频。
- 修复：同章节真实视频扫描提升为第一优先级；顺序续播接纳状态未知的视频但跳过明确已完成视频；原生按钮缩窄为明确“下一视频”；通用“下一节/下一任务/测试”等按钮一律拒绝。

## 跨 iframe 与任务块

- 当前视频和后续视频位于不同 iframe 时，父页面会按照任务块顺序定位当前媒体 ID。
- 后续块必须包含真实视频元素/视频 iframe，或有视频标记且没有测试类文字。
- 测试、题目、测验、作业、签到、考试及 quiz/homework/exam 标记不会成为自动续播候选。

## 验证

- Windows MSTest：29/29 PASS。
- Node 内嵌网页脚本回归：45/45 PASS。
- Python 静态检查：PASS。
- Release Build：PASS，0 warning / 0 error。
- quick-run、自包含 win-x64 与便携 ZIP：PASS。
- 安装器：SKIPPED，未安装 Inno Setup 6；不影响便携包。
- 新增测试：状态未知的同章节后续视频可以顺序播放；视频之间夹着测试题时跳过测试并打开后续视频；通用“下一节”按钮不会被点击。
- 真实学习通账号课程：NOT RUN，仍需宝贝在实际课程验证。

## 归档显示修复

- MASTER TXT 改为 UTF-8 with BOM。Windows 记事本可以直接识别中文编码，不再把文件误判为 ANSI 后显示乱码。
- 内嵌源码载荷、SHA256 校验和安全解压格式保持不变。

安全边界不变：只操作真实视频和真实页面导航；不伪造时长或完成状态；不自动答题、考试、签到；不绕过验证码、人脸、学校认证或风控。
