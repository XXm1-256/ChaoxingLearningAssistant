# v1.30 全课程待播清单与结束复核（2026-09-11）

## 宝贝为何提出、当时要求什么

真实课程已经能够切换下一视频，但“能切一次”不等于整门课程能一直播放完。宝贝要求自动跳过明确已完成章节、越过无视频章节、保留打不开或状态不明的章节，并在最后一个可达视频结束后重新检查全课程，不能过早宣布完成。

## 姐姐怎样处理与 Debug 原理

姐姐新增课程级待播清单，分别记录起点、已核验章节和无法确认章节。Debug 原理是“完成必须有正面证据”：明确完成标记或本轮观察到真实视频自然结束才算处理完成；目录为空、页面证据不足或仍有未解决章节时均保持等待。这样宁可诚实停住，也不误报整课完成。

## 本轮目标

程序从当前起点持续播放时，不再把“暂时找不到下一章”直接当作课程完成。章节中的视频按顺序处理，测验、作业、签到和考试继续排除；无视频章节越过，无法确认的章节保留为未解决项。

## 主要改动

1. 新增 `CoursePlaybackPlan`，按章节顺序生成课程级待核验清单。明确完成章节不进入清单，状态未知章节必须进入核验。
2. 保存本次运行的起始章节、已核验章节和无法确认章节。已经自然结束并穷尽同章后续视频的章节不会被重复播放。
3. 目标页证据可靠、没有播放器且没有可见视频任务时，确认该章没有视频并继续后续章节。
4. 章节无法打开或证据不足时先记录并继续后续章节；最后仍存在该项时停止等待，不误报课程完成。
5. 最后一条可达视频结束后重新扫描完整目录。若发现遗漏待播章节则返回补播；目录为空时也不会显示完成。
6. 没有明确未完成标记时，“开始”可以从第一个状态未知的待核验章节进入，适配不显示进度文字的目录。

## 自动化验证

- Release Build：PASS，0 warning / 0 error。
- MSTest：32/32 PASS。
- Node 适配器回归：46/46 PASS。
- Python 静态检查：PASS。

## 仍需真实课程验收

- 连续跨越多个无视频章节。
- 曾经打不开的章节稍后恢复后能继续核验。
- 学校定制 iframe 中的 `第 N/M 个视频` 总数。
- 从中途起点持续运行到课程末尾并正确显示完成。
