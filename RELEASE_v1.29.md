# v1.29 MASTER 全文编码重建与产品目标固化（2026-09-11）

## 宝贝为何提出、当时要求什么

MASTER 外层标题可读，但较早版本正文仍是乱码；界面也看不出当前是本章第几个真实视频。宝贝要求全部 MASTER 历史可读、视频序号只统计视频并排除测验，同时把“程序一直开着即可播完整门课程”写成最终验收目标。

## 姐姐怎样处理与 Debug 原理

姐姐停止嵌套已经乱码的旧 MASTER，改从可读 Release、变更、错误、测试和说明重新生成正文，再附可校验源码载荷。Debug 原理是编码与内容分开验证：BOM 解决编辑器识别，乱码特征扫描防止有损文本混入，SHA256 与还原脚本证明载荷完整；视频序号只从过滤后的真实视频清单计算。

## 本轮性质

本轮同时修复累计 MASTER 历史乱码，并针对真实反馈补强自动切章、未看视频识别、自动播放和当前视频编号。

## ERR-ARCHIVE-003：MASTER 外层正常、旧版本正文仍乱码

- 现象：v1.28 MASTER 的标题正常，但嵌套保留的 v1.26/v1.27 等旧正文仍含乱码。
- 根因：旧 MASTER 在生成时已发生有损错误转码。给最外层补 UTF-8 BOM 只能改善新标题，不能恢复已经损坏的旧字符串；继续逐版嵌套还会永久复制乱码。
- 修复：v1.29 停止嵌套旧 MASTER 正文，改为从当前可读的 `CHANGELOG.md`、`ERROR_ARCHIVE.md`、`TEST_REPORT.md`、各版 Release、使用说明与产品规格重新构建累计正文。
- 编码：生成脚本本身与最终 MASTER 都使用 UTF-8 with BOM，兼容 Windows PowerShell 5.1 和旧版记事本。
- 完整性：MASTER 继续包含当前源码 ZIP 的 Base64、SHA256 和安全还原脚本；旧版源码包作为 reference 保留在外层源码包中。

## 最终产品目标

新增 `PRODUCT_EXPECTATIONS.md`，明确最终验收目标：程序持续运行时，从当前起点遍历整门课程的全部真实视频；同章视频按顺序播放；测试、作业、签到和考试不进入播放路径；章节结束后进入下一章；最后一个视频自然结束后才报告课程完成。

### 自动切章与未看视频识别

- 章节包含视频任务明细时，候选状态由视频任务汇总：所有视频都明确完成才跳过该章；存在明确未完成或状态未知视频时保留为待播放章节。
- 下一章已经完成时，继续向后寻找下一条待播放章节，不停在已完成章节。
- 状态未知的视频按待核验处理，避免把实际没看的视频漏掉；界面不会把未知状态谎称为明确未完成。
- 自动切章继续使用与左侧列表相同的真实 DOM 点击，并保留网页原生函数及完整学习页导航回退。

### 自动播放

- WebView2 启动参数加入 `--autoplay-policy=no-user-gesture-required`，解除内嵌浏览器默认的用户手势限制。
- 原有 4 轮真实播放器状态确认继续保留；只有读取到 `paused=false` 才算自动播放成功。

### 视频编号

- “播放视频”等模糊标题不再直接展示。
- 当前任务能映射到章节视频清单时，显示 `第 N/M 个视频 · 标题`；无可靠标题时只显示 `第 N/M 个视频`。
- 章节测验、测试题、作业、签到和考试占位不会进入视频任务清单。

仍需继续：整门课程待播清单、无视频章节连续跨越和真实账号长时间验收。

## 验证要求

- MASTER 文件头必须为 `EF BB BF`。
- 可读正文不得含 Unicode replacement character 或旧乱码特征串。
- 内嵌源码必须能通过 `restore_master.py` 校验并恢复。
- C#、Node 与静态检查结果必须继续保持通过。当前结果：MSTest 29/29、Node 46/46、静态检查 PASS。
- Release Build：PASS，0 warning / 0 error；quick-run、自包含 win-x64 与便携 ZIP：PASS。
- 安装器：SKIPPED，当前电脑未安装 Inno Setup 6；不影响便携版。
