# 学习通课程视频播放助手：常见问题与处理方法

适用于 v1.41 Windows 便携版。章节目录和完成状态可能分批出现，点一次按钮后请让程序完成本轮查找。

## 还没手动打开章节，点“打开未完成章节”却说没有

v1.41 会先尝试打开网页目录中的第一个真实章节，让学习通把完整章节目录加载出来，然后自动重新查找。这个过程可能需要几秒，不必先手动点章节，也不必连续点击按钮。程序不会把“返回课程”、章节测验、作业或考试当成用来加载目录的章节。

如果页面本身还没有显示任何章节入口，程序会明确提示目录未能加载。此时请等待学习通页面加载完成后再试一次。

## 点“开始”后暂时没有跳转

学习通有时先显示章节名称，稍后才补充任务状态。程序会自动重读，并在连续三次读到相同目录状态后再选择目标。请先等几秒并观察顶部提示与右侧“下一视频”，无需反复点击“开始”。

## 跳到了显示未完成、但视频已经看完的章节

章节“未完成”可能只是还剩测验、作业或其他任务。程序进入后会核对真实视频；确认视频都已完成后，会继续寻找后面的未看完视频。v1.41 会先等待目录状态稳定，同时忽略网页源码中未显示的旧状态模板，减少同一次查找先错后对的情况。

## 当前视频在播放，但“下一视频”仍显示等待

下一章节的视频信息有时只有进入章节后才出现。程序会先锁定下一候选章节，当前视频结束并进入该章后，再显示并定位具体未完成视频。若等待较久仍未更新，可点一次“开始”。

## 同一章节有多个视频，却没有接着播放

先确认顶部显示“辅助运行中”，并等待当前视频自然结束。程序只在真实播放器确认结束后切换，不会通过修改页面数据伪造完成。若停住，可点一次“开始”；仍无效时导出反馈包。

## 左侧没有课程或章节

先在程序内进入学习通课程首页并完成登录。点左侧课程后，程序会切到章节页签并补读目录。课程为空可点“刷新课程”；章节仍为空时可点一次“打开未完成章节”，程序会尝试打开首个真实章节来加载完整目录。

## 新视频已打开，但没有自动播放

浏览器或学校播放器可能要求一次人工点击。直接点网页中的播放按钮即可；后续程序仍会继续观察和切换。验证码、人脸识别、登录或学校认证出现时，也需要同学亲自处理。

## 怎样反馈问题

打开“日志与反馈”，点“导出反馈包”，同时说明课程页面、点击步骤、当前视频是否正在播放，以及预期跳到哪一段。公开发送前，请检查并删除不便公开的课程名称、学校名称或页面地址参数。

## 使用边界

本程序只辅助操作真实学习通页面，不绕过登录、验证码、人脸识别、学校认证或风控，不伪造观看时长、视频进度、成绩或完成状态，不自动答题、考试或签到。最终学习记录请在学习通页面人工核对。
