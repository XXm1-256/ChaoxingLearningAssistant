# v1.39 跨章节待播目标两阶段锁定 2026 09 12

## 宝贝要求了什么

当前视频正在播放且已经是本章节最后一个视频时，右侧不应显示“未发现待播放的下一视频”。程序应先找到后续未完成视频所在章节，当前视频结束后进入该章，再锁定并播放其中具体的第一条未完成视频。“打开未完成章节”也应使用相同判断。

## 为什么这样要求

学习通的课程目录不一定提前写出章节内任务类型。若只有进入章节后才生成视频 iframe，要求目录必须先证明“这是视频章节”会漏掉真正待播的视频；但仅凭章节未完成直接播放又可能把测验未完成误当成视频未完成，因此必须分两步确认。

## 姐姐怎样修复

- 目录阶段只放行平台明确标为未完成的未知类型章节，状态未知章节仍不盲跳。
- 播放中把该章节显示为下一待核验目标，避免错误显示全课程没有下一视频。
- 当前视频自然结束后进入目标章节，重新扫描真实视频任务并聚焦第一条未完成视频。
- 若进入后发现视频全部完成或没有视频，继续使用既有复核链跳过该章，不回放已完成视频，也不进入测验。

## Debug 原理

将“章节候选”和“具体视频”拆成两阶段证据：课程目录负责确定值得打开的后续章节，目标章节页面负责提供真实视频任务、完成状态和播放器身份。这样既能覆盖懒加载视频，也不会把章节总完成状态直接等同于视频状态。

## 验证结果

- MSTest：39/39 通过，新增两项本轮专用回归。
- Adapter JavaScript：52/52 通过，视频、测验与懒加载识别原有回归保持通过。
- 真实账号中的跨章节预告与自然结束续播仍需使用 v1.39 成品复测。
