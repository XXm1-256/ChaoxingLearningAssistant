# v1.40 目录状态稳定后再选择待播视频（2026-09-12）

## 宝贝要求了什么

- 修复偶尔找到已完成章节的问题。
- 处理“过一会儿又能找到正确位置”的目录加载延迟。
- 更新使用教学，并把常见问题文档放在教学同一目录。

## 为什么这样要求

学习通会先显示章节标题，再异步补充完成状态；页面源码还可能保留未显示的完成/未完成图标模板。过早读取或直接搜索整段源码，会把尚未加载状态或已经完成的章节当成待播目标，造成重复播放和错误跳转。

## 怎么做

- 章节和视频任务的完成状态优先读取当前可见文字。
- 明确的 `jobUnfinishCount=0` 判为已完成，大于 0 判为未完成。
- 隐藏模板同时含“已完成/未完成”时视为无可靠结论，不能覆盖可见状态。
- “开始”“打开未完成章节”和自然结束后的全课程复核若只看到未知状态，会等待并自动重扫最多三次。
- 新增《常见问题与处理方法》Markdown、DOCX、PDF，并与使用教学一起放在用户包最外层。

## Debug 原理

回归夹具故意构造“页面可见已完成，但 `textContent`/`outerHTML` 含隐藏未完成模板”的章节和视频任务。正确结果必须仍为已完成。另以“未完成数量为 0、可见文字无状态”的夹具验证平台计数优先级。延迟读取由 C# 流程在只有未知候选时定时重扫，避免第一次空壳目录直接决定跳转。

## 仍需宝贝实测

- 真实账号中点一次“开始”或“打开未完成章节”，观察目录加载较慢时能否自行找到正确未完成视频。
- 当前章节最后一个视频自然结束后，确认不会进入已完成章节或已完成视频。
- 不同学校私有页面仍只能通过反馈包继续适配。

## 本机验证

- Adapter JavaScript：55/55 PASS。
- MSTest：39/39 PASS。
- Windows Release Build：PASS，0 Warning / 0 Error。
- self-contained win-x64 发布：PASS。
- Inno Setup 未安装，因此安装器跳过；不影响便携 ZIP。
