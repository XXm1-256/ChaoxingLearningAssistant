# 学习通课程视频播放助手 v1.47

## 更新内容

- 修复课程与视频进度条一直空着的问题，当前视频进度随实际播放时间更新。
- 课程显示已完成任务点数和总任务点数，包含测验等非视频任务。只有网页提供明确数量时才显示；尚未读取时不会用视频数或章节数代替。
- 倍速继承只操作当前播放器中可用的倍速选项，不反复点击，也不修改无关下拉框。没有可用选项时尝试恢复正常速度。
- 倍速相关播放等待满 90 秒仍未成功时，最多再尝试一次正常速度播放；再次失败则保留当前视频供手动处理，不跳过视频。

## 使用与常见问题

先登录课程，手动打开准备观看的视频，再点“开始”。切换视频可能需要约半分钟，不要反复点击。每个等待阶段最多 90 秒，成功后立即继续；正常速度重试属于单独的等待阶段。

课程提示“任务点进度尚未读取”：当前网页尚未提供可识别的明确总数。可返回课程列表刷新；该提示不代表零进度，也不影响视频播放。不根据测验完成情况决定自动续播，不自动答题。

右侧显示“正在读取”：视频总时长还未读到，请等待播放器加载。进度条只反映播放位置，不代表平台最终确认的学习记录。

不能使用二倍速：尊重课程播放器限制，使用正常速度。若仍暂停，请手动点播放；遇到验证码、人脸识别或学校认证，应本人处理。

本工具仅辅助正常视频播放，不绕过认证或限制，不伪造学习时长、完成状态或成绩，不自动答题、考试或签到。学习记录以平台为准。

## 验证范围

本地 55 项 .NET 测试和 65 项脚本测试通过，包括实际 WPF 进度条布局、任务点数量解析、倍速目标隔离和禁用选项。未进行登录后的真实课程连续播放验证；不同网页格式仍可能无法读取全部任务点数量。
