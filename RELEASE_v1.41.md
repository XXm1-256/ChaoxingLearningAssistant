# v1.41 空目录自动初始化与稳定查找 2026 09 12

## 宝贝要求

- 课程刚进入、尚未手动打开章节时，点“打开未完成章节”不能直接提示没有。
- 同一次查找不能先选错，重复点几次后才偶然选对。
- 继续排除测验、作业等非视频任务，并维持最小范围修改。

## 修复内容

- 目录为空时，自动寻找首个带真实章节身份的网页目录入口并点击，触发学习通生成完整目录。
- 初始化入口明确排除“返回课程”、章节测验、测试、作业、签到和考试，也排除真实任务点容器。
- 目录初始化后自动继续查找，不再要求同学再次点击或先手动打开章节。
- 每轮查找对章节身份、任务类型、章节完成状态和视频任务完成状态生成完整指纹；连续三次相同才采用，任何变化都会重新计数。
- “开始”“打开未完成章节”及导航完成后的继续流程共用稳定目录读取逻辑。

## Debug 原理

学习通课程页存在两个先后阶段：网页先提供章节入口，打开章节后才建立完整目录与任务状态。v1.40 把空目录误当成已查完，并在看到第一个未完成标记时过早决定目标。本版把处理拆成“初始化目录”和“等待目录稳定”两步：前者只点击平台已经给出的真实章节入口，后者不相信单次快照，连续核对一致后才跳转。

## 自动验证

- Adapter JavaScript 回归：57 项通过，新增初始化时跳过章节测验、拒绝点击返回课程两项。
- MSTest：39 项通过。
- 静态检查通过；Windows Release Build 通过，0 Warning / 0 Error；self-contained win-x64 发布成功。
- 自包含成品隐藏启动 6 秒保持运行，没有发生启动即退出。
- 使用教学 4 页、常见问题 2 页已全部渲染检查，无裁切、重叠、乱码或缺字。
- 用户版 ZIP 共 482 项，最外层为六份教学/常见问题文档和一个程序次级文件夹；开发者版为单一顶层目录并包含累计 MASTER。两包均未发现 Data、Logs、Diagnostics 或 WebView2 运行数据。
- 累计 MASTER 已独立还原 176 个源码与文档文件。
- 真实学习通账号中的空目录与延迟状态仍需宝贝实测，自动化夹具不能代替学校页面验收。

## 建议实测

1. 重新进入一门课程，不先点击任何章节，直接点一次“打开未完成章节”。
2. 等待按钮完成查找，确认程序自行进入真实章节并继续定位未看完视频。
3. 在目录状态出现较慢的课程中只点一次，确认不会先跳已完成章节，也不需要反复点击。
4. 让当前章最后一个视频自然结束，确认相同稳定查找逻辑仍能进入后续真实未完成视频。
