# v1.42 第二视频真实播放与后续章节预告 2026 09 12

## 宝贝要求

- 使用教学改为明确说明：进入课程后，必须先在网页中手动点开任意一个视频，再点击“打开未完成章节”。
- 界面显示“第 2/2 个视频”时，第二个视频必须真正加载并播放，不能停在 `00:00 / 00:00`。
- 当前章节最后一个视频之后，应读取并显示后续待核验章节，不能直接显示“未发现待播放的下一视频”。

## 修复内容

- 自动播放请求改为按新播放器的来源、媒体 ID、页面地址和 DOM 序号定向执行，避免旧视频 iframe 抢走播放请求。
- `00:00 / 00:00`、暂停且没有媒体就绪证据的空播放器不再算作切换成功；程序会重新激活目标视频任务点并继续等待真实播放器。
- 播放器未就绪时，当前视频显示“视频正在载入”，下一视频显示“正在识别下一视频”，不再提前显示错误的 `第 2/2 个视频`。
- 播放期间每隔一段时间补读章节目录。后续章节尚未暴露完成状态时，先显示为“进入后定位未完成视频”的待核验章节，并按课程顺序进入检查。
- 教学与常见问题统一写明先手动点开一个视频的必要步骤，不再宣传课程首页可以可靠自动初始化。

## Debug 原理

学习通可能同时保留旧播放器 iframe，并先创建一个没有时长、没有媒体就绪状态的新 `<video>`。旧逻辑看到播放器身份变化就提前确认第二个视频，再把通用播放请求交给页面中最先找到的播放器，因此可能再次操作旧 iframe。新版把“任务点已点击”“播放器已创建”“播放器可播放”“播放器正在播放”分开核验；只有新播放器具备时长、就绪状态或已经播放，才进入成功分支。播放请求也绑定到刚识别的新播放器。

后续章节的目录状态也可能尚未加载。明确完成章节继续排除，明确测验、作业、考试、签到和资料继续跳过；状态未知的真实章节只作为待核验目标，进入后仍必须扫描真实视频，不能把未知状态伪造成未完成。

## 为什么这样要求

界面编号变化不等于真实视频已经播放。若空播放器被当成成功，自动续播会停住，右侧也会错误认为当前章已经走到最后。真实课程实测还证明课程首页自动初始化并不可靠，因此教学必须给同学一条稳定、可重复的操作路径。

## 自动验证

- Adapter JavaScript 回归新增定向播放测试，58 项全部通过。
- MSTest 新增空播放器就绪判断、未知后续章节预告与按序核验测试，42 项全部通过。
- 静态检查通过。
- Windows Release Build、自包含发布、文档渲染、ZIP 与 MASTER 还原结果见 `TEST_REPORT.md`。

## 实测步骤

1. 登录并进入课程，先在网页中手动点开任意一个视频。
2. 点“打开未完成章节”或“开始”。
3. 让同章节第一个视频自然结束，确认第二个视频出现真实时长并自动播放。
4. 第二个视频播放时，确认右侧显示后续章节或具体下一视频，而不是直接显示没有下一视频。
5. 让第二个视频自然结束，确认程序进入后续章节并核验真实视频，测验和已完成章节不会被当成视频播放。
