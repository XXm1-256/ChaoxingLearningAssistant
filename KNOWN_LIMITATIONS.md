# v1.44 当前边界

- 自动续播只操作真实学习通页面中的真实视频/任务点，不伪造观看时长或完成状态。
- 平台不暴露完成状态时，程序不会把未知状态猜成未完成；自然播放结束后可按真实目录顺序进入状态未知的下一章。
- 学校私有播放器若完全隐藏视频、任务容器、章节 ID 和可关联 iframe，仍需依据真实诊断日志增加模板适配。
- 浏览器媒体策略可能允许切换到新视频但拒绝自动 `play()`；程序会保留新页面并提示用户点击播放。
- 课程缓存只用于避免短暂扫描失败导致空列表，不替代网页实时结果；缓存有效期为 30 天。
- 统计时间来自约 1.5 秒一次的播放器观察，属于辅助统计而非平台官方学习时长；超过 15 秒的采样间隔视为休眠/阻塞，不累计。
- 没有可信发布源，因此本版移除未接入界面的更新检查空壳，不提供虚假的“已是最新版”结论。
- v1.44 由同学在学习通网页中手动选择起始视频；程序只接管当前视频并在自然结束后继续下一段。
- v1.44 已覆盖空播放器误判、定向播放新 iframe、后续未知章节预告与按序核验；不同学校的私有任务模板与账号数据仍需实机验收。
- 学校定制页面的视频编号、多个无视频章节连续跨越和真实账号长时间连续播放仍未完成验收。

## Windows 必验

1. `BUILD_V1_44.bat` 完成 Restore / Release Build / MSTest / quick-run publish。
2. 真实课程中验证课程缓存、章节单击、多视频任务、2x 自然结束续播和登录恢复。
3. 播放—暂停—恢复—换视频后检查今日统计，确认暂停时间不计入且每个视频独立成行。
4. 导出反馈包，确认 snapshot 中包含真实播放器 source/mediaId/taskKey/chapterId 等字段。

本轮修复与验证事实见 `RELEASE_v1.44.md`；最终目标见 `PRODUCT_EXPECTATIONS.md`，真实平台行为仍需课程实测。
