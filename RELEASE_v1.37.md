# v1.37 章节视频完成状态二次核验 2026 09 12

## 宝贝要求了什么

点击“打开未完成章节”时，不能只看章节整体是否完成。章节可能因为测验或作业没有完成而显示未完成，但其中视频已经全部看完。此时应继续寻找真正含有未看完视频的章节。视频自然结束后的顺序跳转也必须遵守相同规则。

## 为什么这样要求

作品的目标是保持开启后按真实视频顺序持续播放完整门课程。若把“章节未完成”直接当成“视频未完成”，程序会停在已经看完的视频上，甚至重新播放，既打断连续播放，也可能漏掉更后面的未看完视频。

## 姐姐怎样修复

- 候选章节打开并出现播放器后，重新扫描该章真实视频任务。
- 只有至少一条视频未完成或状态仍需核验时，才在该章停下并尝试播放。
- 若所有真实视频都有完成证据，则把该章标记为视频已核验，继续寻找后续章节。
- 没有任务明细时，仅接受明确标记为视频且未完成的章节，普通章节总状态不再替代视频状态。
- “打开未完成章节”和自然结束后的自动顺序跳转都经过同一个章节准备流程，因此修复同时覆盖两条路径。

## Debug 原理

测试场景专门构造为“两条视频均已完成，但章节仍因测验未完成”。正确结果必须是跳过该章，而不是重新播放。同时保留空任务列表为待核验状态，避免页面加载较慢时被误判成全部完成。日志新增 `CX-COURSE-VIDEOS-COMPLETE`，用于确认程序为何跳过某章并继续向后查找。

## 验证结果

- MSTest：37/37 通过。
- Adapter JavaScript：52/52 通过。
- 静态检查、完整构建、两份 ZIP、MASTER 和 GitHub 同步结果见 `TEST_REPORT.md`。
- 真实学习通账号与学校定制页面仍需成品实测，自动测试不会替代平台结果。
