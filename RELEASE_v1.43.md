# v1.43 连续播放静态缺陷加固 2026 09 12

## 宝贝要求

- 根据 v1.42 的独立静态审查修复能够静态确认的缺陷，不把理论风险或未登录页面推测写成实测通过。
- 保持最小改动，不重写稳定界面和无关模块。
- 修复后继续提供用户版、开发者版、累计 MASTER、教学和常见问题。

## 修复内容

- 播放请求与状态核验从头到尾绑定最初播放器的文档、DOM 位置、媒体编号和媒体路径；明确冲突时不再用序号兜底到其他播放器。
- 播放重试只读取目标播放器快照，不再被仍在播放的旧 iframe 带走。
- 真实 video 与尚未创建 video 的 iframe 统一使用父任务块顺序；重复 mediaId 且 iframe 地址不同的任务不再合并。
- 同章结果拆为“没有候选”“切换成功”“存在候选但切换失败”。切换失败会停住提示，不把当前章写入已核验集合。
- 目标任务尚未形成播放器时也会按有界次数重新激活，不再要求先看见空播放器才重试。
- 跨章节和手动切换统一要求播放器真正就绪；目录或任务清单未稳定时保持待核验，不提前宣告完成。
- 增加轻量操作代次和交叉入口忙碌判断；停止辅助或开始新操作后，旧异步结果不能继续确认播放。
- 播放轮询持续检查验证码、人脸、学校认证和登录失效提示，识别后暂停辅助。
- 日志及反馈包统一遮盖 token、enc、openc、签名、认证头和 Cookie；旧日志打包时也会重新脱敏。
- 完整构建写入版本和 EXE 哈希标记；用户包只接受与当前项目版本及标记完全一致的发布目录。
- MASTER 恢复增加重复路径、CRC 和临时目录校验；开发包明确排除运行数据目录。
- 开发接手基线由过期的 v1.39 更新为 v1.43。

## Debug 原理

播放器切换不是一个瞬间动作，而是“点击任务点、创建 iframe、创建 video、加载媒体、提交 play、实际播放”六个阶段。旧实现部分路径只要看到新元素就算成功，且后续重试可能重新选择页面中更醒目的旧播放器。v1.43 保存不可变目标：已有证据必须一致，空播放器可以在同一 DOM 位置继续补齐来源和时长，但不能漂移到其他文档或序号。

章节遍历也把“没有下一视频”和“切换动作失败”分开。前者才允许继续跨章；后者保留当前章为未确认并停止自动推进，从状态上杜绝漏播。

## 为什么这样要求

界面显示新编号并不能证明真实媒体已经切换。若失败与不存在共用同一个布尔值，程序会把技术失败误当成课程事实，进而跳过仍未播放的视频。反馈包还会被同学主动分享，因此网址中的临时鉴权参数必须在写入文件之前遮盖。

## 自动验证

- Adapter JavaScript 回归覆盖同来源旧播放器、目标 DOM、混合懒加载任务顺序及未知状态顺序。
- MSTest 覆盖目标播放器匹配、重复 mediaId、真实扫描 TaskKey 的 CDN 签名轮换和日志脱敏。
- Windows Release Build、自包含发布、启动冒烟、文档、ZIP 与 MASTER 结果记录在 `TEST_REPORT.md`。

## 仍需真实课程验证

- 学校页面从第一个视频自然结束后，第二个视频能否真实加载、显示时长并自动播放。
- 当前章最后一个视频结束后，后续未知章节能否进入并定位第一条真实待播视频。
- 外层已隐藏但内部仍报告尺寸的旧 iframe 是否存在于目标学校模板。
