# v1.32 GitHub 开源发布与外层图文教学（2026-09-11）

## 宝贝为何提出这一版

普通下载包打开后直接出现大量 DLL、配置和程序文件，对非技术使用者不友好；同时需要一份适合 GitHub 公开阅读、可以协作维护的源码，以及一个在 GitHub 页面上可以直接下载运行的成品。

## 宝贝本轮要求

1. 下载包最外层先看到图文使用教学和一个次级文件夹，正常程序文件放到次级文件夹。
2. 制作可以在 GitHub 公开发布的开源版本。
3. 制作 GitHub 上可以直接下载使用的 Windows 版本。
4. 完成 GitHub 仓库和 Release 上传。
5. 继续采用自包含、累计归档，说明要求、原因、处理方式与 Debug 原理。
6. 最终只交付两份 ZIP：GitHub 用户版不含归档；开发者版必须包含累计归档。
7. 图文教学以同学们为直接读者，不写开发打包过程；明确工具边界、非官方关系和使用责任。

## 姐姐怎样处理

- 制作自包含的 `使用教学.html`，用界面示意图、六步卡片、播放顺序图和故障提示讲清使用方法；另附纯文字 Markdown 版本。
- Windows 下载包采用两层结构：外层只放教学与版本说明，`学习通课程播放辅助` 次级文件夹内才放 EXE、DLL 和运行文件。
- 开源仓库加入 MIT 许可证、贡献规则、安全说明、隐私排除 `.gitignore` 和 GitHub Actions 验证流程。
- 公开源码排除构建产物、日志、诊断包、WebView2 用户数据、登录信息、旧版 ZIP 和 MASTER 大文件。
- GitHub Release 使用与本机验证一致的自包含 win-x64 程序重新组包，普通使用者无需安装 .NET SDK。
- 两份 ZIP 严格分流：用户版只含教学和运行文件；开发者版含源码、MASTER、CHANGELOG、ERROR_ARCHIVE、TEST_REPORT 与历版 Release。
- 用户教学增加免责声明：不绕过认证、不伪造观看或成绩、不自动答题/考试/签到，平台结果需要人工核对，使用者应遵守学校与课程规则。

## Debug 原理

发布验证分成四层：源码隐私扫描、编译与自动化回归、下载包结构检查、解压后启动检查。仓库可编译不代表下载包易用，下载包存在也不代表目录结构正确，因此每一层必须分别验证。GitHub 页面只在本地文件确定无敏感内容后才上传。

发布前宝贝补充了关键实机线索：同章视频自然结束后停住，但再次点“开始”可以进入下一视频。姐姐据此把问题收窄到两条路径的差异——“开始”会重新扫描任务，“自然结束”原本依赖相对位置。部分学习通模板把每个视频放在独立 iframe，导致子页面媒体地址无法定位父页面任务顺序。修复只改这一段：由扫描结果明确指定同章下一视频，并继承父任务块序号；原相对查找继续作为回退，没有搬入无关开源实现。

首次组装下载包时，Windows PowerShell 5.1 把无 BOM 脚本里的中文路径误读并导致语法错误。姐姐没有改掉中文教学，而是让脚本本身保持 ASCII 安全：按扩展名寻找教学文件，并在运行时用 Unicode 码点生成中文次级文件夹名，从根因上消除系统代码页差异。

修复后 Release 编译达到 0 warning / 0 error，MSTest 32/32、Node 适配器 47/47、静态检查与隐私扫描全部通过。最终 ZIP 顶层只有两份教学和一个中文次级文件夹；从该 ZIP 实际解压的 EXE 启动 5 秒保持运行。安装器因没有 Inno Setup 6 明确记为 SKIPPED。

最终交付严格只有两份面向本轮的 ZIP：`ChaoxingLearningAssistant_v1.32_Windows.zip` 已上传 GitHub Release，作为不含开发归档的用户版；`ChaoxingLearningAssistant_v1.32_Developer.zip` 是本地交付的开发者版，包含源码、累计 MASTER、错误档案、测试报告和历版 Release，未上传 GitHub。公开仓库与下载页分别是 `https://github.com/XXm1-256/ChaoxingLearningAssistant` 和 `https://github.com/XXm1-256/ChaoxingLearningAssistant/releases/tag/v1.32`；仓库简介只介绍功能与使用，不写审美设计。

## 安全与边界

本项目不伪造观看时长或完成状态，不自动答题、考试、签到，不绕过验证码、人脸、学校认证或平台风控。公开反馈不得附带账号、Cookie、完整课程身份参数或未脱敏日志。真实学习通账号从课程起点连续运行至最后视频仍需实机验收。
