# v1.27 自动切章与自动播放修复说明（2026-09-11）

## 宝贝为何提出、当时要求什么

真实使用中，点左侧章节虽然能切页面，但视频不会自动开始；自然播放结束后也不会可靠进入后续未看章节。宝贝要求手动切章后自动播放、自动跳过明确完成章节，并且只有真实播放器变化后才能更新左右状态。

## 姐姐怎样处理与 Debug 原理

姐姐把“目录点击成功”和“播放器真正切换”拆成两层证据：点击只是请求，媒体身份变化才算完成。新播放器加载较慢时有界重试，并读取真实 `paused` 状态确认播放。Debug 原理是两阶段提交：先操作网页，再核对播放器；失败便保留旧界面并尝试网页原生动作或安全导航回退。

## 本轮目标

修复两条直接影响连续观看的流程：视频自然结束后没有自动进入下一章节；手动点击左侧章节后只完成切换、没有自动开始播放。本轮只增强真实网页导航和真实播放器控制，不伪造进度或完成状态。

## 根因与修复

### ERR-AUTO-NEXT-008：状态未知的下一章被排除

- 现象：当前视频结束后，目录看得到后续章节，但程序可能报告没有下一章或停在原处。
- 根因：旧流程要求候选章节必须带有“明确未完成”标记。部分学习通目录只给顺序和章节 ID，不给完成状态，于是正常的下一章被过滤掉。
- 修复：把“寻找未完成章节”和“自然结束后按顺序找下一章”分开。前者继续只认平台明确标记，后者按目录顺序选择第一个不是“明确已完成”的可导航章节，并跳过测验、作业、签到和考试。
- 网页原生目录回退同样先选明确未完成项，再接纳状态未知项，不再把未知项错误命名为“未完成”。

### ERR-AUTO-PLAY-004：切换成功后只尝试一次播放

- 现象：播放器已经切到下一视频，但停在暂停画面。
- 根因：旧流程在播放器刚换载时只调用一次播放。学习通的新播放器、iframe 或媒体源尚未就绪时，这次调用容易失败。
- 修复：统一增加“等待并确认播放”流程。新播放器最多进行 4 轮有界重试，每轮重新读取真实 `paused/ended` 状态；播放成功才进入 Playing 状态。
- 倍速继承也放进重试流程，在播放器真正出现后再恢复。
- 若浏览器最终明确拒绝自动播放，章节仍保留在已切换位置，并只给一次人工点击提示。

### ERR-MANUAL-PLAY-002：左侧手动切章没有接续播放步骤

- 现象：点击左侧章节后播放器切换，但不会自动播放。
- 根因：手动切章的各个成功分支只确认章节、刷新列表和轮询，没有调用播放流程；顶层导航回退完成后也没有保存“切完后播放”的意图。
- 修复：目录点击、网页原始章节函数、当前学习页直接导航和真实 href 导航四条路径全部汇入同一个“确认章节并自动播放”收尾流程。
- 对需要整页导航的路径，跨导航保存目标章节与旧播放器快照，页面加载并确认目标播放器后再播放，避免只更新左栏高亮。

## 验证结果

- `dotnet test ChaoxingLearningAssistant.sln -c Release`：PASS，29/29。
- `node --test scripts/adapter_regression.mjs`：PASS，43/43。
- `python scripts/static_check.py`：PASS。
- 新增回归：网页原生下一章候选在完成状态未知时仍按顺序返回，同时保持 `CompletionKnown=false`。
- Release Build：PASS，0 warning / 0 error。
- quick-run publish：PASS。
- self-contained win-x64 publish 与便携 ZIP：PASS。
- 安装器：SKIPPED，当前电脑未安装 Inno Setup 6；不影响便携包。
- 真实学习通账号课程：NOT RUN。当前没有失败电脑上的日志，平台私有模板仍需宝贝实测确认。

## 实测时重点观察

1. 视频自然结束后，网页播放器真实进入下一章并自动播放。
2. 手动点左侧不同章节后，新章节播放器自动开始。
3. 目录没有“未完成/已完成”文字时，仍能按顺序续到下一章，但“打开未完成”不会把未知状态冒充未完成。
4. 若仍失败，保留 `CX-AUTO-NEXT*`、`CX-AUTO-PLAY`、`CX-MANUAL-CHAPTER*`、`CX-PLAYER-SWITCH` 与 `CX-CHAPTER*` 日志。

安全边界不变：视频必须真实播放；不伪造观看时长或完成状态；不自动答题、考试、签到；不绕过验证码、人脸、学校认证、风控或权限。
