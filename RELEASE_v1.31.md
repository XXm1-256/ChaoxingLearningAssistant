# v1.31 雾森护眼界面、操作反馈与可重复教学（2026-09-11）

## 宝贝为何提出这一版

旧界面偏深色蓝紫，长时间挂着观看不够柔和；按钮按下不明显，功能开关后也缺少即时确认。对不熟悉技术细节的使用者而言，只看到状态文字很难判断操作是否真正生效。近几版归档也逐渐变成改动清单，遗漏了需求背景、实现方法和 Debug 原理。

## 宝贝本轮要求

1. 采用浅绿为主、护眼、有科技感和呼吸感的界面，特效不能省。
2. 按钮按下要有明显反馈；开始、暂停、停止、辅助开关、宽屏和设置保存要有提示。
3. 增加教学，首次运行自动显示，之后仍可从主界面重新打开。
4. Debug 后交付，不能只凭界面看起来正常。
5. v1.26–v1.30 归档补齐“为何要求、要求内容、怎样实现、Debug 原理”，本版继续累计且自包含。

## 姐姐怎样实现

- 默认主题改为浅绿“雾森”色系：灰白绿背景、深绿文字、薄荷绿强调色；深色选项同步改成森林色系。
- 保留渐变、环境光、发光阴影和圆角玻璃卡片；特效集中在边缘和状态区域，避免遮挡网页视频。
- 全局按钮按下时缩小到 95%、下沉 2 像素，并改变底色与描边。
- 主窗口增加右上提示卡。开始、暂停、停止、辅助开关、宽屏和设置保存均会说明结果。
- 设置页开关在勾选或取消时立即显示“已开启/已关闭（保存后生效）”。
- 首次教学扩展为四步，说明登录、选课、状态确认、同章视频顺序、测验跳过和安全暂停；顶部 `?` 可随时重新打开。

## Debug 原理与真实过程

本轮采用“编译器 → 单元测试 → 静态守卫 → 发布构建”的分层验证：

1. 第一次编译发现 WPF Trigger 无法定位 `TransformGroup` 内部对象。修复为对按钮边框整体替换 `RenderTransform`，保持视觉效果并符合模板名称作用域。
2. 第二次编译发现 WPF 与 WinForms 都定义了 `CheckBox`。修复为显式使用 `System.Windows.Controls.CheckBox`。
3. 第三次测试发现旧断言仍要求无效主题回退深色。产品默认已改为护眼浅绿，因此同步把测试期望改成 Light；这属于需求变化后的验收更新。
4. 全套 MSTest 随后达到 32/32 PASS，网页适配器回归 46/46 PASS，v1.31 静态防退化检查 PASS。
5. 首次运行正式构建脚本时，沙箱无权读取用户目录中的 `NuGet.Config`，Restore 中止。授权只读该配置后重新执行，Restore、Release Build、MSTest 和 quick-run 发布全部通过。这说明失败来自构建环境权限，不是作品源码。
6. 自包含 win-x64 发布和便携 ZIP 生成通过；安装器因本机没有 Inno Setup 6 而明确记为 SKIPPED，没有冒充成功。
7. 第一次从 v1.31 MASTER 反向还原时发现脚本仍查找 v1.30 载荷标记，因此真实恢复失败。姐姐同步标记、增加静态守卫并重新生成；第二次真实还原通过，恢复 116 个文件且载荷 SHA256 一致。这正是“必须实际还原，不能只看文本可读”的 Debug 原理。
8. quick-run 成品隐藏启动 5 秒保持运行，日志确认 v1.31 与 WebView2 Runtime 152 正常初始化，并进入登录等待状态；没有出现打开即崩溃。该检查结束后主动关闭测试进程。

## 为什么采用这种方案

浅绿只承担环境和状态，不给 WebView2 网页加滤镜，避免课程视频失真。按钮视觉反馈与功能结果提示分开：按压反馈证明点击发生，提示卡说明程序状态已经改变。教学复用同一窗口，维护简单，也允许随时复习。

## 安全与边界

本版只调整应用界面、反馈和教学，没有伪造播放进度，没有自动答题、考试或签到，也没有绕过验证码、人脸、学校认证和平台风控。Figma Starter 额度在设计读取阶段到顶，因此实现以已确认的浅绿方案和现有 WPF 结构为准；最终界面仍需在真实 Windows 缩放比例下目视验收，真实学习通账号连续播放也仍需实机验收。
