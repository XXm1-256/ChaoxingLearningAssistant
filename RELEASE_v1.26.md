# v1.26 正式修复说明（2026-09-11）

## 宝贝为何提出、当时要求什么

v1.25 在另一台 Windows 电脑点开后会生成错误报告，而且旧 MASTER 有乱码。没有现场日志时，先恢复“源码能编译、错误能复现、归档能读懂”的可靠基线，不能继续靠猜测叠加功能。宝贝要求修复启动错误、让中文归档可读，并从此记录需求原因、处理方法和验证事实。

## 姐姐怎样处理与 Debug 原理

姐姐先用真实编译器定位语法断裂，再用单元测试暴露“不同媒体路径却被当成同一视频”的逻辑错误。Debug 原理是分层取证：编译错误先解决语法链；业务错误用最小输入对照媒体身份；最后用 29 项测试和 42 项网页脚本回归证明修复没有破坏旧行为。详细证据与边界保留在下文。

## 本轮结论

v1.24 与 v1.25 共用一处 C# 语法错误，导致 Windows 无法完成构建。v1.25 的静态检查和 42 项 JavaScript 回归无法发现该问题，因为两者都不是真正的 C# 编译。v1.26 已修复语法错误，并在 Windows 10/11 桌面环境完成 Release 编译和 MSTest。

## 已修复问题

### ERR-BUILD-006：启动后只得到 BUILD_LOG，无法生成 EXE

- 现象：运行 `START.bat` 后自动进入构建，随后打开错误日志，程序没有启动。
- 根因：`MainWindow.xaml.cs` 的播放器轮询逻辑中，一条赋值语句被放在 `if` 与 `else if` 之间，编译器报告 5 个语法错误。
- 影响版本：确认 v1.24、v1.25 源码均受影响。
- 修复：恢复完整的 `if / else if` 结构，再在分支结束后保存播放器快照。
- 防回归：静态检查增加错误结构守卫；更重要的是，正式归档前必须执行真实 `dotnet build/test`。

### ERR-MEDIA-001：不同视频可能因复用 mediaId 被误判为同一视频

- 现象：播放器地址从 `a.mp4` 切到 `b.mp4`，但 mediaId、文档地址和 DOM 位置相同，旧逻辑仍可能判断为同一视频。
- 根因：旧逻辑把可被平台复用的 mediaId 当成足够强的证据。
- 修复：CDN 只有查询参数变化时仍视为同一地址；路径真正变化时，必须有一致 TaskKey 才能继续认定为同一任务。
- 防回归：新增“查询参数轮换”和“路径变化”组合测试。

### WARN-TEST-001：MSTest 使用过时标记

- 将 `DataTestMethod` 改为当前 MSTest 支持的 `TestMethod + DataRow`，清除构建警告。

## Windows 实际验证

- .NET SDK：8.0.424。
- Release 编译：PASS，0 error / 0 warning。
- MSTest：29/29 PASS。
- Node 适配器回归：42/42 PASS。
- quick-run 发布：PASS，生成 `artifacts\quick-run\ChaoxingLearningAssistant.exe`。
- self-contained win-x64 便携包：PASS，可在未安装 .NET 8 Desktop Runtime 的电脑上运行。
- WPF 主窗口启动：PASS；窗口标题为 v1.26，主要控件正常呈现。
- WebView2 初始化与学习通登录页：PASS；未填写账号或提交登录。
- 真实学习通账号与课程：NOT RUN；仍需在实际课程页面验证章节识别、点击切换和自然结束续播。

## 归档修复

- `CHANGELOG.md` 补回 v1.24、v1.25、v1.26 的独立记录。
- `ERROR_ARCHIVE.md` 补回错误编号、现象、根因、影响版本、修复和验证状态。
- `TEST_REPORT.md` 补回真实执行环境、命令、结果和未执行边界。
- 新增 v1.26 验证摘要及原始测试输出。
- 累计 MASTER 保留旧历史，并追加本轮完整事实和可恢复源码载荷。

## 日志位置

- 构建失败：源码根目录 `BUILD_LOG.txt`。
- 普通安装运行日志：`%LOCALAPPDATA%\ChaoxingLearningAssistant\Logs`。
- 便携版运行日志：程序目录 `Data\Logs`。

安全边界不变：视频必须真实播放；不伪造观看时长或完成状态；不自动答题、考试、签到；不绕过验证码、人脸、学校认证、风控或权限。
